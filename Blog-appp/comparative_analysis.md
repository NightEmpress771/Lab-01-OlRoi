# 📝 Порівняльний Аналіз Реляційної та Документної Схем

## 1. Реляційна Схема (SQL/Нормалізація)
Схема базується на **7 таблицях** (`users`, `categories`, `posts`, `tags`, `post_tags`, `comments`, `post_history`), пов'язаних за допомогою зовнішніх ключів (FOREIGN KEY).
* **Перевага:** Висока цілісність даних (ACID), мінімальна надмірність.
* **Недолік:** Складність читання. Для отримання повного посту (з автором, категорією, тегами та коментарями) потрібен **складний JOIN** через 5-6 таблиць, що уповільнює найчастіший запит.
* **Масштабованість:** Переважно вертикальна (нарощування потужності сервера).

## 2. Документна Схема (MongoDB/Денормалізація)
Схема базується на **3 основних колекціях** (`users`, `categories`, `posts`). Ключові дані (коментарі, автор, категорія) **вбудовані** у документ `posts`.
* **Перевага:** Висока швидкість читання. Більшість даних для відображення посту отримується **одним запитом** (`db.posts.findOne()`), виключаючи дорогі JOIN-операції.
* **Недолік:** Надмірність даних (наприклад, зміна імені автора вимагає оновлення в усіх його постах). Потребує програмної підтримки цілісності (наприклад, при зміні slug категорії).
* **Масштабованість:** Горизонтальна (шардинг) – ідеально підходить для вебзастосунків з великим обсягом даних.

## 3. Висновки Продуктивності для Блог-Системи

| Критерій | Реляційна модель (SQL) | Документна модель (MongoDB) |
| :--- | :--- | :--- |
| **Отримання повного посту** | Повільно (багато JOIN) | **Дуже швидко** (один I/O-запит) |
| **Складні аналітичні запити** | Швидко (оптимізатор, індекси) | Швидко (Pipeline Aggregation, менше JOIN) |
| **Додавання/Оновлення (запис)** | Швидко (через нормалізацію) | Повільніше (оновлення вбудованих даних) |
| **Гнучкість/Зміни схеми** | Низька (потрібні міграції) | **Висока** (схема без схем) |
| **Обраний підхід** | **Менш оптимально** для читання. | **Оптимально**, фокусується на швидкому читанні. |

**Висновок:** Для **блогу**, де операція **читання** (отримання посту) виконується на порядки частіше, ніж операція **запису** (додавання/оновлення), **документна схема MongoDB є ефективнішою** завдяки патерну **декомпозиції** та уникненню складних JOIN.